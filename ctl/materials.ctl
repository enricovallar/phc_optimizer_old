; ==============================================================================
; materials.ctl
; Common material definitions and helper functions for MPB
; ==============================================================================

(define air (make dielectric (epsilon 1.0)))
(define silicon (make dielectric (epsilon 12.0)))
(define sio2 (make dielectric (epsilon 2.1316)))
(define gaas (make dielectric (epsilon 13.0)))
